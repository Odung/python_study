# black jack
# N개의 숫자 중에 3개를 뽑아 더한 합이 M에 가장 가까운 합만 출력하기, 중복 없음
# 모든 조합을 확인하는 코드를 짤 것
# recursion 이용

M = 3
numbers = [5, 6, 7, 8, 9, 10]
is_visited = [False] * len(numbers)
print(is_visited)

def solve(is_visited):

    pass